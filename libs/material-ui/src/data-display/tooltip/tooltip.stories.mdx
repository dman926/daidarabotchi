import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Tooltip } from './tooltip';
import Button from '../../inputs/button/button';

<Meta
  title="Material-UI/Data Display/Tooltip"
  component={Tooltip}
  argTypes={{
    placement: {
      options: [
        'bottom-end',
        'bottom-start',
        'bottom',
        'left-end',
        'left-start',
        'left',
        'right-end',
        'right-start',
        'right',
        'top-end',
        'top-start',
        'top',
      ],
      control: {
        type: 'select',
      },
    },
  }}
/>

export const BasicTemplate = (args) => (
  <Tooltip {...args}>
    <span style={{ fontSize: '2rem' }}>
      <Button>Hover Over Me!</Button>
    </span>
  </Tooltip>
);

export const NonHoverableTemplate = (args) => (
  <Tooltip {...args}>
    <span style={{ fontSize: '2rem' }}>
      <Button>Focus On Me!</Button>
    </span>
  </Tooltip>
);

# Tooltip

Full description - https://mui.com/material-ui/react-tooltip

<Canvas>
  <Story
    name="Primary"
    args={{
      title: 'Delete',
    }}
  >
    {BasicTemplate.bind({})}
  </Story>
  <Story
    name="Non Hoverable"
    args={{
      title: 'You have focused on the button!',
      disableHoverListener: true,
    }}
  >
    {NonHoverableTemplate.bind({})}
  </Story>
  <Story
    name="Tooltip With Arrow"
    args={{
      title: 'An example of a tooltip with an arrow.',
      arrow: true,
    }}
  >
    {BasicTemplate.bind({})}
  </Story>
  <Story
    name="Positioned Tooltip"
    args={{
      title: 'This tooltip is positioned to the right!',
      placement: 'right',
    }}
  >
    {BasicTemplate.bind({})}
  </Story>
  <Story
    name="Longer Tooltip Delay"
    args={{
      title: 'This tooltip takes longer before it appears and disappears.',
      enterDelay: 1000,
      leaveDelay: 1000,
    }}
  >
    {BasicTemplate.bind({})}
  </Story>
</Canvas>
