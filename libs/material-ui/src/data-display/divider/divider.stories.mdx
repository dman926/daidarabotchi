import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Divider } from './divider';
import List from '@mui/material/List';
import ListItem from '@mui/material/ListItem';
import ListItemText from '@mui/material/ListItemText';
import { grey } from '@mui/material/colors';

<Meta title="Material-UI/Data Display/Divider" component={Divider} />

export const HorizontalTemplate = (args) => (
  <List
    sx={{ width: '300px', border: `1px solid ${grey[400]}` }}
    component="nav"
    aria-label="mailbox folders"
  >
    <ListItem button>
      <ListItemText primary="Inbox" />
    </ListItem>
    <Divider {...args} />
    <ListItem button>
      <ListItemText primary="Drafts" />
    </ListItem>
  </List>
);
export const VerticalTemplate = (args) => (
  <List
    sx={{
      border: `1px solid ${grey[400]}`,
      display: 'flex',
      alignItems: 'center',
      width: '300px',
      p: 0,
    }}
    component="nav"
    aria-label="mailbox folders"
  >
    <ListItem button>
      <ListItemText primary="Inbox" />
    </ListItem>
    <Divider {...args} />
    <ListItem button>
      <ListItemText primary="Drafts" />
    </ListItem>
  </List>
);

# Divider

Full description - https://mui.com/material-ui/react-divider

<Canvas>
  <Story
    name="Horizontal"
    parameters={{ controls: { exclude: ['orientation'] } }}
  >
    {HorizontalTemplate.bind({})}
  </Story>
  <Story
    name="Horizontal Inset Or Subheader"
    args={{ variant: 'inset' }}
    parameters={{ controls: { exclude: ['orientation'] } }}
  >
    {HorizontalTemplate.bind({})}
  </Story>
  <Story
    name="Horizontal Middle"
    args={{ variant: 'middle' }}
    parameters={{ controls: { exclude: ['orientation'] } }}
  >
    {HorizontalTemplate.bind({})}
  </Story>
  <Story
    name="Horizontal With Text"
    args={{ textAlign: 'center', children: 'Text' }}
    parameters={{ controls: { exclude: ['orientation'] } }}
  >
    {HorizontalTemplate.bind({})}
  </Story>
  <Story
    name="Vertical"
    args={{ orientation: 'vertical', flexItem: true }}
    parameters={{ controls: { exclude: ['orientation'] } }}
  >
    {VerticalTemplate.bind({})}
  </Story>
  <Story
    name="Vertical Middle"
    args={{ orientation: 'vertical', variant: 'middle', flexItem: true }}
    parameters={{ controls: { exclude: ['orientation'] } }}
  >
    {VerticalTemplate.bind({})}
  </Story>
  <Story
    name="Vertical With Text"
    args={{
      orientation: 'vertical',
      textAlign: 'center',
      children: 'Text',
      flexItem: true,
    }}
    parameters={{ controls: { exclude: ['orientation'] } }}
  >
    {VerticalTemplate.bind({})}
  </Story>
</Canvas>
