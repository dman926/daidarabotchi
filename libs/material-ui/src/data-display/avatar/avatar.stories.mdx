import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Avatar } from './avatar';
import { Badge } from '../badge/badge';
import { styled } from '@mui/system';
import userImage from '../../../assets/images/christina.jpg';

<Meta title="Material-UI/Data Display/Avatar" component={Avatar} />

export const StyledBadge = styled(Badge)(({ theme }) => ({
  '& .MuiBadge-badge': {
    backgroundColor: '#44b700',
    color: '#44b700',
  },
}));

export const NonBadgeTemplate = (args) => <Avatar {...args} />;
export const BadgeTemplate = (args) => (
  <StyledBadge
    anchorOrigin={{ vertical: 'bottom', horizontal: 'right' }}
    overlap="circular"
    sx={{ color: 'secondary.main' }}
    variant="dot"
  >
    <Avatar {...args} />
  </StyledBadge>
);

# Avatar

Full description - https://mui.com/material-ui/react-avatar/

<Canvas>
  <Story
    name="Image"
    args={{
      alt: 'Remy Sharp',
      src: userImage,
      sx: { bgcolor: 'primary.main' },
    }}
  >
    {NonBadgeTemplate.bind({})}
  </Story>
  <Story
    name="Image And Badge"
    args={{
      alt: 'Remy Sharp',
      src: userImage,
      sx: { bgcolor: 'primary.main' },
    }}
  >
    {BadgeTemplate.bind({})}
  </Story>
  <Story
    name="Letters"
    args={{
      alt: 'Remy Mitchell',
      src: '/broken-image-path.jpg',
      sx: { bgcolor: 'info.main' },
    }}
  >
    {NonBadgeTemplate.bind({})}
  </Story>
</Canvas>
