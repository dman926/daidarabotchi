import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Link } from './link';

<Meta title="Material-UI/Navigation/Link" component={Link} />

export const Template = (args) => <Link {...args}>I am a Link!</Link>;

#

Full description - https://mui.com/material-ui/react-link

<Canvas>
  <Story
    name="Primary"
    args={{
      color: 'primary',
      underline: 'always',
      variant: 'inherit',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
