import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Pagination } from './pagination';

<Meta title="Material-UI/Navigation/Pagination" component={Pagination} />

export const Template = (args) => <Pagination {...args} />;

#

Full description - https://mui.com/material-ui/react-pagination

<Canvas>
  <Story
    name="Primary"
    args={{
      count: 10,
      color: 'primary',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
