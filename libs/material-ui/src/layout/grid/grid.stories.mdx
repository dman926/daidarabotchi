import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Grid } from './grid';
import Paper from '../../surfaces/paper/paper';
import { styled } from '@mui/material';

<Meta title="Material-UI/Layout/Grid" component={Grid} />

export const Item = styled(Paper)(({ theme }) => ({
  backgroundColor: theme.palette.mode === 'dark' ? '#1A2027' : '#fff',
  ...theme.typography.body2,
  padding: theme.spacing(1),
  textAlign: 'center',
  color: theme.palette.text.secondary,
}));

export const Template = (args) => (
  <Grid {...args} container spacing={2}>
    <Grid item xs={8}>
      <Item>xs=8</Item>
    </Grid>
    <Grid item xs={4}>
      <Item>xs=4</Item>
    </Grid>
    <Grid item xs={4}>
      <Item>xs=4</Item>
    </Grid>
    <Grid item xs={8}>
      <Item>xs=8</Item>
    </Grid>
  </Grid>
);

# Grid

Full description - https://mui.com/material-ui/react-grid

<Canvas>
  <Story name="Primary">{Template.bind({})}</Story>
</Canvas>
