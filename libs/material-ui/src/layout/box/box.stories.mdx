import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Box } from './box';

<Meta title="Material-UI/Layout/Box" component={Box} />

export const Template = (args) => <Box {...args} />;

# Box

Full description - https://mui.com/material-ui/react-box

<Canvas>
  <Story
    name="Primary"
    args={{
      sx: {
        width: 300,
        height: 300,
        backgroundColor: 'primary.dark',
        '&:hover': {
          backgroundColor: 'primary.main',
          opacity: [0.9, 0.8, 0.7],
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
