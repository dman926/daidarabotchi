import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Container } from './container';
import { Box } from '../box/box';

<Meta
  title="Material-UI/Layout/Container"
  component={Container}
  argTypes={{
    maxWidth: {
      options: ['xs', 'sm', 'md', 'lg', 'xl'],
      control: {
        type: 'select',
      },
    },
  }}
/>

export const Template = (args) => (
  <Container {...args}>
    <Box sx={{ bgcolor: '#cfe8fc', height: '100vh', width: '5rem' }} />
  </Container>
);

# Container

Description Here

<Canvas>
  <Story name="Primary">{Template.bind({})}</Story>
</Canvas>
