import { Meta } from '@storybook/addon-docs';
import MuiSvg from '../assets/images/mui.svg';
import packageJson from '../package.json';

<Meta
  title="Material-UI/Overview"
  parameters={{ viewMode: 'docs', previewTabs: { canvas: { hidden: true } } }}
/>

<div style={{ textAlign: 'center' }}>

<img src={MuiSvg} width={128} />

# Material-UI

<code>{`version ${packageJson['version']}`}</code>

This is a wrapping library to [MUI](https://mui.com/).

</div>
