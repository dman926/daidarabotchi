import { Canvas, Meta, Story } from '@storybook/addon-docs';
import CircularProgress from './circular-progress';
import {
  BasicTemplate,
  InteractiveTemplate,
  MultipleColorTemplate,
  MultipleDeterminateTemplate,
  WithLabelTemplate,
} from './story-templates';

<Meta
  title="Material-UI/Feedback/Circular Progress"
  component={CircularProgress}
  argTypes={{
    color: {
      options: ['primary', 'secondary', 'error', 'warning', 'success', 'info'],
      control: { type: 'radio' },
    },
  }}
/>

export const Template = (args) => <CircularProgress {...args} />;

# Circular Progress

Description Here

<Canvas>
  <Story name="Basic">{BasicTemplate.bind({})}</Story>
  <Story name="Color" args={{ color: 'primary' }}>
    {MultipleColorTemplate.bind({})}
  </Story>
  <Story name="Determinate">{MultipleDeterminateTemplate.bind({})}</Story>
  <Story name="Interactive">{InteractiveTemplate.bind({})}</Story>
  <Story name="With Label">{WithLabelTemplate.bind({})}</Story>
</Canvas>
