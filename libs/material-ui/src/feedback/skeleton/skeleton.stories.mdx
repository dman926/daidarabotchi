import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Skeleton } from './skeleton';
import { PrimaryTemplate, DemoTemplate } from './story-templates';

<Meta title="Material-UI/Feedback/Skeleton" component={Skeleton} />

# Skeleton

Description Here

<Canvas>
  <Story
    name="Rectangular"
    args={{
      width: 300,
      height: 300,
      variant: 'rectangular',
      animation: 'pulse',
    }}
  >
    {PrimaryTemplate.bind({})}
  </Story>
  <Story
    name="Circular"
    args={{
      width: 300,
      height: 300,
      variant: 'circular',
      animation: 'pulse',
    }}
  >
    {PrimaryTemplate.bind({})}
  </Story>
  <Story
    name="Text With Wave"
    args={{
      width: 1000,
      height: 50,
      variant: 'text',
      animation: 'wave',
    }}
  >
    {PrimaryTemplate.bind({})}
  </Story>
  <Story
    name="Demo"
    args={{
      width: 1000,
      height: 50,
      variant: 'text',
      animation: 'wave',
    }}
  >
    {DemoTemplate.bind({})}
  </Story>
</Canvas>
