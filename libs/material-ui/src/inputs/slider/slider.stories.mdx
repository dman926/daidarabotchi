import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Slider } from './slider';

<Meta title="Material-UI/Inputs/Slider" component={Slider} />

export const Template = (args) => <Slider {...args} />;

# Slider

Full description - https://mui.com/material-ui/react-slider

<Canvas>
  <Story name="Primary" args={{ sx: { width: '7.5em' } }}>
    {Template.bind({})}
  </Story>
</Canvas>
