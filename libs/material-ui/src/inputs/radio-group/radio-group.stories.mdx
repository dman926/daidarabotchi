import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { RadioGroup } from './radio-group';
import { Radio } from '../radio/radio';
import { FormControl, FormLabel, FormControlLabel } from '@mui/material';

<Meta title="Material-UI/Inputs/Radio Group" component={RadioGroup} />

export const Template = (args) => (
  <FormControl>
    <FormLabel>Gender</FormLabel>
    <RadioGroup {...{ ...args, defaultValue: 'female' }}>
      <FormControlLabel value="female" control={<Radio />} label="Female" />
      <FormControlLabel value="male" control={<Radio />} label="Male" />
      <FormControlLabel value="other" control={<Radio />} label="Other" />
    </RadioGroup>
  </FormControl>
);

# Radio Group

Full description - https://mui.com/material-ui/react-radio-button/#radio-group

<Canvas>
  <Story name="Primary">{Template.bind({})}</Story>
</Canvas>
